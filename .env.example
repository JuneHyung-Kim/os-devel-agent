# =============================================================================
# Code RAG Agent Configuration
# =============================================================================

# -----------------------------------------------------------------------------
# API Keys
# -----------------------------------------------------------------------------
GEMINI_API_KEY=AIz...
ANTHROPIC_API_KEY=sk-ant-...

# -----------------------------------------------------------------------------
# Embedding Model Configuration (for vector search)
# -----------------------------------------------------------------------------
# Options: gemini, default, ollama
EMBEDDING_PROVIDER=default

# Model name for embeddings
# Ollama: mxbai-embed-large, nomic-embed-text, all-minilm
EMBEDDING_MODEL=mxbai-embed-large

# -----------------------------------------------------------------------------
# Chat Model Configuration (for AI conversations)
# -----------------------------------------------------------------------------
# Options: gemini, claude, ollama, openai
CHAT_PROVIDER=gemini

# Model name for chat
# Gemini: gemini-1.5-flash, gemini-1.5-pro, gemini-2.0-flash-exp
# Claude: claude-sonnet-4-6, claude-opus-4-6, claude-haiku-4-5-20251001
# Ollama: llama3.2, qwen2.5:14b, deepseek-r1:8b, etc.
# OpenAI / vLLM: model name as loaded in the server (e.g. Qwen/Qwen2.5-72B-Instruct)
CHAT_MODEL=gemini-1.5-flash

# -----------------------------------------------------------------------------
# Ollama Configuration (for local models)
# -----------------------------------------------------------------------------
# Base URL for Ollama server
OLLAMA_BASE_URL=http://localhost:11434

# -----------------------------------------------------------------------------
# OpenAI-compatible Configuration (for vLLM or other OpenAI-compatible servers)
# -----------------------------------------------------------------------------
# Base URL of the OpenAI-compatible endpoint (e.g. vLLM server)
OPENAI_BASE_URL=http://localhost:8000/v1
# API key — vLLM does not require a real key; any non-empty value works
OPENAI_API_KEY=EMPTY

# -----------------------------------------------------------------------------
# Other Settings
# -----------------------------------------------------------------------------
PROJECT_ROOT=./

# API Call Confirmation (development safeguard)
# When true, prompts before each LLM API call: [Y/n/all]
CONFIRM_API_CALLS=true

# -----------------------------------------------------------------------------
# LangSmith Tracing (optional)
# -----------------------------------------------------------------------------
LANGCHAIN_TRACING_V2=true
LANGCHAIN_API_KEY=lsv2_pt_...
LANGCHAIN_PROJECT=code-rag-agent